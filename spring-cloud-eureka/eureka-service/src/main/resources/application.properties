spring.application.name: service1
server.port: 0

# Enable actuator access without security checks - DEVELOPEMENT ONLY
# management.security.enabled: false   Spring Boot 1.5 property
management.endpoints.web.exposure.include: '*' 
eureka.client.serviceUrl.defaultZone: http://localhost:8761/eureka/
eureka.instance.leaseRenewalIntervalInSeconds: 5
#eureka.instance.instance-id: ${spring.cloud.client.ipAddress}:${server.port}

#cloud.consul.host: 1.1.1.100
#cloud.consul.port: 8500
#cloud.consul.discovery.healthCheckPath: /test
#cloud.consul.discovery.healthCheckInterval: 5s
#spring.cloud.consul.discovery.instanceId